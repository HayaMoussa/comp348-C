// Libraries
#include <stdio.h>
#include "io.h"

void test2(int *columns) {
    printf("int value = %d, Character = %c\n", columns[0], columns[0]);
    printf("int value = %d, Character = %c\n", columns[1], columns[1]);
    printf("int value = %d, Character = %c\n", columns[2], columns[2]);
}